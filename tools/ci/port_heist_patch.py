"""Temporary comparison-runner input capture, not a runtime change."""
raise SystemExit('Pinned input captured as a diagnostic artifact. Implementation patch follows; do not merge this preparatory commit.')
