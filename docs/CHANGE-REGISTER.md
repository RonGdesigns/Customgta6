# Bloodlines — Change Register

**Status:** Living reconciliation table, maintained by the implementation agent.  
**Companion to:** `AGENT-HANDOFF-PROPOSED-CHANGES.md`, `CAMPAIGN-IMPLEMENTATION-AUDIT.md`  
**Branch:** `claude/handoff-implementation-pass` (September 9, 2026)

This is the table the audit asked for (§17): one row per proposal item, what the
code and data actually say, what was decided, and whether the game has confirmed
it. "Live verified" means played in GTA V Enhanced on the installed build. Nothing
in this pass has been live-verified yet; the source-level checks that did run are
listed in `HANDOFF-IMPLEMENTATION-UPDATE.md`.

## Source priority applied

1. Latest explicit user-approved creative decision (the handoff's locked wording).
2. Current working implementation where it does not conflict with that decision.
3. Latest authored mission/dialogue data (`story_beats.txt`, `opening_scene.txt`, `dialogue_edits.json`).
4. Proposal documents.
5. Older bible/PDF descriptions.

## Register

| Mission / system | Current implementation (this branch) | Current authored data | Approved intended change | Old proposal / bible conflict | Live verified? |
| --- | --- | --- | --- | --- | --- |
| Prologue (LSIA → home) | `PrologueSequence`: Guess deploys solo at `Prologue.LSIACurb`, arrival scene with phone/walk/enter-car blocking, player drives to the starter-apartment entrance, homecoming scene (exit car, walk to door, read the job), fade and time-cut to Terminal Island, then M01 starts. Hold Backspace skips and still marks it played. `CampaignState.PrologueComplete`; saves that already finished M01 are exempt. | `M01:prologue` (4 lines) and `M01:arrival` (3 lines) in `scenes.tsv`, all GUESS. | Ron is the first point of view; physical walk and vehicle entry; control returns seated; drive home; exit and job setup; transition into M01. | Older proposal assumed Burro Heights exterior because apartments were thought exterior-only. The audit (§9) reversed that: the implemented starter apartment is the destination. Burro Heights remains a fallback through `CrewHomes.Position`. | No. Needs: LSIA curb/car coordinates, walk path, car entry animation, apartment marker arrival, M01 placement after the time-cut, watch vs skip. |
| M01 vehicle | `schafter3` (four-door) in `CutsceneDirector` and `M01GhostInTheDockyard`. | Bible text says "prototype supercar"; authored lines say "four-door prototype". | At least four seats; no two-seat T20. | Bible's T20 wording. | Resolved in an earlier pass; unchanged here. |
| End of implemented content | `CampaignState.Progress()` distinguishes five states: story available / story gated / side content only / story blocked / all implemented content complete. Mission key and menu report the state; nothing falls back to M01. `MarkComplete` logs the end of content. | — | Separate states, never M01 as a fallback. | Audit §4.3 described a fallback that the audited snapshot no longer had; the state reporting was still missing and is now explicit. | No (source test only). |
| Retry after failure | Mission key prefers `LastAttempted` while `RetryAvailable`; a nearby marker explicitly chosen overrides; the player is told which job is retrying. | — | Direct retry of the failed mission before ordinary selection. | — | No. |
| Rewards / replay | All cash, gold, safehouse and fleet flags commit once in `CampaignState.MarkComplete` → `AwardCompletion`. M22's foundry loss and gold reset commit only on first completion. Story-test proves M24 twice pays once. | — | Idempotent, first-completion-only story mutations. | Audit §4.1/4.2 described stage-time awards; those were already moved before this branch. | No. |
| SM01 ammunition | New flag `armorPiercingSupply`; `WeaponProgression.RestockCount` doubles Ice's rifle restock at home lockers. SM01's completion subtitle says exactly that. | SM01 outro unchanged. | Decide whether the AP ammo is real. Decision: a locker-restock supply line, Ice only. | Bible implied a persistent ammo type the engine does not expose. | No. |
| SM03 racing transmission | `FleetGarage.FitChopBay`: vehicles repaired at Guess's chop bay get the race transmission mod once each. Wired through `CrewHomes.ApplyFleetUpgrade`. | — | Trace the flag; implement, describe honestly, or remove. Decision: implement at the chop bay. | Flag existed with no consumer. | No. |
| Mission-issued weapons | Superseded by the *Weapon loans* row below: capture is paused while a loan is open **and** the loan is returned at teardown. | — | Classify loans vs permanent; milestones must not be pre-empted. | — | No. |
| Pre-reunion free roam | Deploy key before M01 completes deploys Guess alone (`DeploySolo`); dev builds keep the full sandbox. | — | Story players cannot field the trio before the reunion. | — | No. |
| Required assets | `Mission.RequireAsset(entity, reason)` fails the attempt the tick a required vehicle/prop is lost, independent of which stage is protecting it. Applied to M19 Kraken, M20 Cargobob + container, M21 launch + Cargobob + container, M22 Cargobob, M24 crane. | — | Every required asset gets fail / respawn / alternate / invulnerability. | — | No. |
| Port Heist continuity | `OperationHandoff` / `HandoffLedger` on `MissionContext`. M19 records the surfaced Kraken → M20 puts Gohan in it; M20 records the loaded lift → M21 starts the Cargobob where the climb-out ended **and always attaches a visible container**; M21 records → M22 consumes. Records live in memory only; a restart falls back to default staging with a log line. | — | Preserve or reconstruct the same apparent state; never escort an empty lift. | — | No. Needs: attached container under an AI-flown Cargobob at 45 m, escort behavior. |
| Cutscene motion | `SceneBlocking` steps: WalkTo, EnterVehicle, ExitVehicle, UsePhone, LookAt, Wait, with timeouts and `Finish()`. `CutsceneDirector.Play(..., blocking)` leaves movers unfrozen, tracks the current subject, holds the scene until blocking finishes, completes remaining steps on `Skip()` and cancels them on `Stop()`. | Prologue/arrival scenes use it. | Reusable moving-scene helpers; skip ≡ watch. | — | No. Needs: GoTo/EnterVehicle task behavior at the curb, camera clipping. |
| Gohan decision mechanic | `TechnicalChoiceObjective` (cycle with G / D-pad Left, commit with E / D-pad Right). M28: which relay system to cut first changes response delay and squad size. | M28 gameplay lines unchanged. | A small set of reusable Gohan decisions rather than timers. | — | No. |
| M02–M07, M10, M14, M16, M27, M30, SM02, SM03 dialogue | `dialogue_edits.json` revised; `dialogue.tsv` regenerated. No "switch to", markers, button names, radii or "no ability needed" in speech for those missions (story-test enforced). | Regenerated. | HUD = mechanics, dialogue = people. | Bible's tutorial-style lines. | No. |
| M05 reveal | Mateo: three contracts, "they wanted all three of you there", city money behind it, no full conspiracy. Ice: "Then we find out who." | Outro already said the accusation is not proof. | Allegation and lead only; M46 stays the hard-evidence payoff. | Bible's forty-million-dollar contract reveal. | No. |
| M10 | Locked: "I joke when I'm nervous too. Learn the difference." | `M10_SCENE_OUTRO_02_GUESS` | Locked wording. | "scared". | No. |
| M25 | Locked: "…hear the pressure in my voice. Next time, I'll make the call." | `M25_SCENE_OUTRO_01_ICE` | Locked wording. | "hear me scared". | No. |
| M42 | Locked: "Sub deployed. That one had my nerves up. Nobody put that in the flight log." | `M42_SCENE_OUTRO_01_GUESS` | Locked wording. | "I'm still shaking". | No. |
| M17 | "External release, where either of you can reach it." | `M17_SCENE_OUTRO_01_GOHAN` | Remove "marked yellow" from speech. | — | No. |
| M22 strike limitation | Gohan: the strike came off a contract airframe with a city permit and their address on file; Aegis spent the one shot it can explain, the next needs a target it can prove. | `M22_SCENE_OUTRO_01_GOHAN` | World logic for why the strike cannot simply repeat. | — | No. |
| M26 aircraft | Guess: the Lazer towed out of Zancudo under the M16 clearance, fueled at McKenzie. | `M26_SCENE_INTRO_01_GUESS` | Establish acquisition/storage or change aircraft. | Aircraft appeared only because the mission needed it. | No. Mission code still spawns the Lazer at the airfield; a visible hangar aircraft between M16 and M26 is future work. |
| M45 | "Helipad's secure. Gohan, I need Bradley's card up here. Guess, how's our bird?" | `M45_SCENE_OUTRO_01_ICE` | Conversational, not commander. | — | No. |
| SM05 / SM06 / SM07 / SM08 aftermaths | Rewritten to the proposal's short forms (Ice's "come eat before Ron starts calling"; "You actually slowed down."; "Sterling's dead." / "You good?" / "Nah."; "Files are out… You coming back?"). SM08 reframed to communication, not belonging. | Regenerated. | Quieter, brotherly, no therapy language. | Previous authored versions over-explained. | No. |
| M70 ending | Quiet boat: names in words, Gohan distracted by a pinging relay, the phone joke, breakfast. | `M70_SCENE_OUTRO_*` | No confession scene. | Previous authored confession. | No. |
| Solo story gates | `CampaignState.StoryGates`: M19 needs SM01–SM03, M44 needs SM04–SM06, M63 needs SM07–SM08, M68 needs SM09. `NextPlayable` prefers the next main mission and returns the first playable outstanding solo only when a gate is waiting on it. `MissionManager.Start` refuses a gated mission and names the remaining jobs; the dev menu passes `bypassGates`. Grandfathered when the gate mission or any later main mission is already complete. A required solo with **no script still counts**: progress reports `StoryBlocked` by unavailable required content, the text names the missing job, nothing is offered once only unscripted jobs remain, and only dev mode bypasses. | — | Solos optional inside their window, mandatory before the event they set up; old saves never trapped. | Earlier `NextPlayable` picked the first eligible mission in catalog order, so a newly unlocked solo became "next". | No. |
| Weapon loans | `WeaponProgression.BeginLoan/EndLoan`: the locker is snapshotted when a mission starts; on pass (after completion commits), fail, abort, failed start and shutdown, every weapon on a hero that is not in the baseline, the hero's standard loadout or an earned milestone reward is removed from the hero and from the locker. Capture is also suspended while a loan is open. | — | Real loan lifecycle: pre-owned stays, explicit rewards stay, issued weapons never become ownership. | The previous fix only paused capture during the mission; the weapon rode into free roam and was captured there. | No. |
| Cutscene skip vs cancel | `CutsceneDirector.Skip()` finishes unplayed blocking (watch ≡ skip). `Stop()` is cancel: abort, error, watchdog, death, teardown call it and unfinished blocking is `Cancel()`ed, never completed. Enter and controller A call `Skip`; everything else calls `Stop`. | — | An error halfway through Ron's walk is not a skip. | Earlier every `Stop()` completed the blocking. | No. |
| `SceneStep.Finish` | Every `Finish` leaves the documented end state already true: exit-vehicle uses `ExitVehicleStep.ForceOut` (warp-out, then immediate clear + warp-out, then the raw native) and places the actor on a navmesh-checked spot beside the car facing its heading; if the engine still refuses, the step is marked `Failed` and `SceneBlocking.Complete` cancels the rest of the skip instead of running later steps against a false state. Walk places on the mark after a collision request; phone clears immediately; entry seats now. `Cancel` clears the running task and moves nobody. The prologue's cold-open hand-off uses the same `ForceOut` before repositioning; if that fails the vehicle travels to the dock with the player and the log says so. | — | Finish means done, not requested. | `ExitVehicleStep.Finish` used to start a leave task. | No. |
| M21 lift timing | The loaded Cargobob is held frozen, rotors up, from setup until the escort stage begins; `ReleaseLift` then `StartCargobob`. Cleanup and failure release the hold so an occupied lift is never left pinned. Cold start still attaches the container. | — | Nothing flies toward the breakwater while Gohan boards. | Setup used to start the flight immediately. | **No — must be live-tested:** AI heli behavior with an attached container, and the frozen-then-released transition. |
| `TechnicalChoiceObjective` input | The arrival frame consumes both buttons and commits nothing; Detonate is disabled each frame the panel is open and read through `IS_DISABLED_CONTROL_JUST_PRESSED`, so cycling cannot throw a detonator. | M28 speech has no button text; the HUD label does. | Stale-input and native-control suppression. | — | No. |
| OperationHandoff wording | Class summary and this register now say what each receiver restores (M20: vehicle position/heading; M21: vehicle position/heading plus always-attached cargo; M22: consume and log). Hero positions, seats, health, clock and weather are recorded, not restored. | — | Precise claims. | Earlier text implied full restoration. | No. |
| Prologue continuity | Ron tries the two old numbers and gets voicemail from both, so M01's "tried your old numbers" is true. Ron remains alone. | `M01_SCENE_PROLOGUE_02/03` | Reconcile with M01. | "Not tonight" contradicted M01. | No. |
| M27 dispatch | Ice: "You got me out. Next time I tell you both the whole plan…" Other dispatches audited; no further speaker contradictions found. | `CampaignDispatches` | Correct perspective. | "We got you out" from the man who was extracted. | No. |
| M08 / M09 / M12 speech | "the sentries are yours", "keep the rear escort in your sights", "Sonar sweep first", "Keep the sub deep". M03/M04/M06 briefings lose "marked"/button text. | Regenerated. | HUD = mechanics. | — | No. |
| Speech check scope | The story test now scans every scripted mission's gameplay and scene lines with a regex for switch-to, marked/marker, button names, D-pad, "no ability needed", "within N meters" and "radius"; "marking" and a real checkpoint are deliberately spared. | — | Future missions cannot regress. | Previous test covered 13 missions. | n/a |
| M53 / M55 / M57 / M64 | Shortened to the ask or the behavior: "When I ask if you're hurt, I want the real answer."; "I've done enough keeping you two in the dark."; "You had exits planned for us before we ever asked. Keep doing that."; "Nobody decides for the other two." | `*_SCENE_*` regenerated | Less explaining. | Previous lines narrated the insight. | No. |
| M70 gameplay and final line | S1_01 "Tonight we hold this ground.", S1_02 "Third wave! Ice, still with me?", S1_06 "Told y'all it still had engines." (the setup, mid-fight). The boat aftermath ends with the **locked, user-approved** final line, after the breakfast exchange and with nothing after it: **"Told y'all... Guess never misses an exit."** (`M70_SCENE_OUTRO_10_GUESS`). | Regenerated. | The payoff is the last thing said in the campaign. | An earlier pass had removed the line as self-praise; that was wrong — it is locked. | No. |
| M67 / M68 | **Deferred.** Reviewed; kept as authored. M67's "nobody has to save the only copy of me" is Gohan's own idiom and short; M68's "counting three" exchange is already behavior, not explanation. Revisit when the missions are scripted and the lines can be heard against gameplay. | unchanged | — | — | n/a |
| Later character pass (M31–M70 remainder) | **Deferred.** The M01–M70 character pass is not finished: only the items named in the handoff and in this pass were touched. Everything else in M31–M70 keeps its current authored text until those missions are scripted. | unchanged | — | — | n/a |
| Shipped locations template (N1) | `config/Bloodlines.Locations.ini` now ships comments only; `lint_missions.py` fails if it ever carries a key again. `LocationBook` ignores a per-install override whose value is byte-identical to the retired September 8 template (nine M01/Base keys and their headings), logs each one, and counts them in `IgnoredStaleOverrides`; anything a player captured still applies. | — | Repository position corrections must reach existing installs. | The template installed as "the player's survey" overrode every later M01 correction, including the lookout move, on every install. | No — check `Bloodlines.log` for "stale template override" and 157 estimates on load. |
| Prologue route (R01) | `Step("prologue")` moved inside the `ObjectiveMarkers` frame so the drive-home destination becomes the GPS route. | — | Every navigation producer runs between BeginFrame and EndFrame. | It ran before BeginFrame, which cleared it. | No. |
| Refused start side effects (R03) | `MissionManager.CanStart` is side-effect-free; `BloodlinesMain.StartMission` asks it before StandDown; `Start` asks it again. | — | A refused start keeps crew, position, weather, wanted state. | StandDown ran first. | No. |
| Briefing failure loan (R04) | `ForceFail` during a pending briefing finishes the attempt (closes the loan, offers retry) instead of leaving it half-open. | — | Every accepted start ends exactly once. | The loan stayed open with nothing running. | No. |
| Scene outcome contract (R02) | `CutsceneDirector.LastOutcome` (Completed / Skipped / Canceled / Failed). `SceneBlocking.Update` cancels the rest when a timed-out step cannot reach its end state; `Succeeded` is exposed. The prologue: a canceled or failed homecoming returns to the drive, the next arrival places Ron directly, an unseatable Ron is asked to get out, `Finish` fires once. `PlaceForColdOpen` returns Placed / StillSeated / NoGround and moves nothing on failure; the host acts on it and fades back in through `finally`. | — | Failure is never success; every phase is recoverable. | Cancel and error were indistinguishable from completion. | No. |
| M28 camera option (R05) | "Yard cameras first" now leaves the response unalerted: squads arrive on guard and engage on sight instead of being sent at the crew; the consequence text says so. A consequence that throws fails the objective instead of completing on a promise. | — | Options do what they promise. | The option was the baseline with a promise attached. | No. |
| Map generator (R06) | `audit_campaign.py` cuts each stage at its own statement and resolves objectives handed in by field name; it also reads `StoryGates` and prints the gate under the prerequisite. | Regenerated. | The map reflects the source. | The choice appeared under Ice's stage. | n/a |
| QA keys (N2) | `[` / `]` commit / restore checkpoints. Insert was ScriptHookVDotNet's reload-all-scripts key. | — | QA row 5 can be run. | — | No. |
| CI coverage (R07) | A Windows `verify` job runs the story and regression suites, parser tests, both freshness checks and a prebuilt shape check. Byte identity of the prebuilt with a fresh build remains a local check. | — | The badge covers the suites the reports cite. | Only build, lint, coordinates and two doc diffs ran. | Watch the first run. |
| Stale instructions (R10, N3) | `CLAUDE.md` solo rule rewritten to match the gates; `CAMPAIGN-REMAINDER.md` SM07 row matches the M63 gate; `PLAYTEST.md` has a current entry block above the historical script, including the dev-mode note about pre-reunion deployment. | — | One current entry point. | — | n/a |
| M33 trigger | Not changed; M33 has no script yet. Register note: the code acquisition trigger must follow Ramos's stabilization objective. | — | Check the trigger when M33 is scripted. | — | n/a |
| Cypress destruction presentation | Not changed. `M22` commits the safehouse loss; no persistent damage props yet. | — | Decide post-M22 presentation. | — | Open. |
| Checkpoints | Unchanged: `SupportsCheckpointRestore=false` everywhere; retry is a full restart. | — | Do not enable until reconstruction exists. | — | n/a |
| Companion death policy | Unchanged: any hero named by a stage objective is mission-critical; `ComposedMission` fails on their death. Free-roam recovery is suspended while `MissionActive`. | — | Declare per mission. | — | Open: no per-mission "recoverable" tier yet. |
| Briefing cast (Package A, notes 15/22) | `CutsceneDirector` stages each speaking hero as a temporary actor in a short arc facing the start point when no crew ped exists for the slot, and hides the story character for the scene; both are released on every exit. A deployed crew that is merely far away is still a radio call. | — | Briefings show the brothers, never Franklin standing in for them. | The director framed the only ped it had: the story character. | No. |
| Control diagnostics (Package A, notes 9/10/17) | `ControlDiagnostics.Snapshot` writes one `CONTROL [...]` line at scene end, at gameplay start and at the prologue hand-off: ped, active slot, control flag, frozen/invincible/dead, vehicle and seat, gate, companion state, scene outcome, apartment state, collision. Never throws. | — | "I couldn't drive" comes with the state that produced it. | — | No. |
| Marker names (Package A) | Map icons are named `M01 — Title` (solo: `M02 — Owner: Title`). | — | Notes can cite a mission by id. | `Bloodlines: Title`. | No. |
| QA objective completion (Package A, note 12) | `Objective.ForceComplete`, `ComposedMission.CompleteCurrentObjective` (the active hero's objective first), `MissionManager.CompleteObjective`, dev menu "Complete current objective". The stage still advances through its own tick, so exit effects run. Checkpoint entries name their keys and say "Restart this stage" honestly. | — | A blocked objective can be stepped past without pretending. | — | No. |
| Coordinate readout (Package A, note 8) | `DevTools` draws X/Y/Z/heading and, in a vehicle, speed in km/h and mph at the top right instead of in the subtitle strip. | — | Objective text no longer hides the Y of the bay Ron is reading. | — | No. |
| Road handling baseline (Package B, notes 18/20) | `RoadHandling` applies a per-class profile (car / SUV / truck) to each model's shared handling once, alongside the doubled gearing: traction curve up, traction loss down, damping up, center of mass lowered, brake force up, yaw inertia up; restored on stand-down where the value is still ours. Bikes, boats, aircraft, trains take none. Values in `HANDLING-CALIBRATION.md`. | — | Speed target unchanged; steering and weight fixed first. | The gearing was doubled with stock grip and brakes. | No. Paired-loop numbers pending. |
| Guess overlay (Package B, note 21) | `SlipstreamReflex` adds, on his car only while on all wheels, a bounded press toward the road (0.25 g weight plus speed-squared downforce to a 0.6 g cap, as force: 3.6 has no per-vehicle gravity) and a +20% lift on the model's shared traction, restored on deactivate, vehicle change, switch, death, abort and stand-down. The press blends in and out over half a second through the new `Ability.Settle` hook that `AbilityController` runs after deactivation. Never on aircraft or boats. | — | Franklin-style: excellent steering, weighted to the ground. | Time scale only. | No. |
| Briefing arrival (round two) | When no crew ped exists for a briefing, Guess is staged beside where the story character stood and the other speakers arrive in the crew's four-door (`schafter3`): spawned on the street behind the start point, driven to the nearest curb by a `DriveUpStep`, the first line held until the car has pulled up (`SceneBlocking.DialogueAfterStep`). A skip lands the car on the mark. No street within reach falls back to the on-foot staging. | — | The brothers drive up to Guess; they do not appear. | Temporary cast stood in an arc. | No. |
| M04 chase (round two) | Miller gets driver ability and aggressiveness 1.0 and a kept vehicle flee mission from Guess at 40 m/s, reckless, wrong way allowed. The drive pickup plays a reach-in animation (`MissionInteraction.ReachInside`) facing the car. On pass the chase car and Miller's car are released, not deleted. | — | A chase that drives away; a pickup that looks like one; the car Guess got out of is still there. | Cruise task obeyed lights; mission teardown deleted the unoccupied chase car. | No. |
| M05 start (round two) | `MissionSites.Ground` asks the map for the ground height under an estimated coordinate before the walkable check, so a perch 40 m above the authored guess resolves. The refusal names the point and says to survey it with F11. | `M05.CliffPerch` unchanged (estimate). | M05 starts. | "No walkable mission surface: M05.CliffPerch" refused the mission at the marker. | No. Retest at the M05 marker. |
| M06 air waves (round two) | `HeliInsertion`: a police Maverick per two troopers, spawned high on an approach line, flown to a hover over the alley, troopers rappel, aircraft leaves; each phase has a clock and a forced unload so the wave can always end. Waves two and three fly in (two aircraft each, the rest of the wave by street); the first aircraft's arrival is a one-line moment on the pilot. | — | Later SWAT waves arrive by helicopter, the first on camera. | All waves spawned standing in the alley. | No. |
| Apartment loading (round two) | `ApartmentAccess` moves the frozen, invincible, faded-out player into the room as soon as the interior is pinned and judges readiness from there: the interior's own report, or the player already in that room with collision loaded. | — | The starter apartment loads. | Waiting for readiness from the street timed out after 12 s (log: interior 55042). | No. |
| Weapon rewards (round two) | `WeaponProgression.RewardMissions` covers 33 of 36 playable jobs (all but M01, M21, M26); each hero's reward line is unique to him; SM02 pays Gohan and SM03 pays Guess. The shop locks a rewarded weapon until its job is done; stock guns, starting rifles and the ray weapons stay purchasable. | — | Earn most weapons through missions instead of paying. | Nine reward missions. | No. |
| QA mission order (round two) | The dev menu starts any scripted job; `MissionManager.CanStart` skips the prerequisite check only under the QA bypass and logs it. The mission key still enforces order. | — | Any mission from the debug menu. | The menu refused unmet prerequisites. | No. |
| Vehicle damage (visuals session, reviewed) | Per-class deformation, collision and engine multipliers on the model's shared handling through `RoadHandling`, restored with the road profile. Crew protection is per vehicle instance in `WorldTuning`: applied when a brother is aboard, removed when the car is empty of them, the player's modifier following the car they drive, all restored on stand-down. | `[VehicleDamage]` in `Bloodlines.ini`. | GTA IV-style crumple without instant engine failure for the crew. | The hand-over wrote crew protection into shared handling by whoever sat in the first registered car of the model, and never reset the player's modifier. | No. |
| Visual atmosphere (visuals session, reviewed) | `VisualAtmosphere`: timecycle grade per time band with ini name overrides and a log line per change; shadows and level-of-detail set once and capped at 2.0; blur off per frame; water reflection distance on the player and their car, per entity; ocean swell in free roam only; full `Reset` on abort and reload. The "ceramic clearcoat" key was removed: its native is the burnt-paint overlay. | `[Visuals]` in `Bloodlines.ini`; `docs/VISUALS.md`. | Native clarity with honest costs. | Water-reflection native called without its entity; paint native misused; LOD, ocean and reflection state never reset; `DeSmog` parsed but unused; ocean swell on during boat missions. | No. Modifier names still unverified against game data. |
| Shipped ini (visuals session, reviewed) | The package ships `Bloodlines.ini.example` and `Bloodlines.Locations.ini.example`; the installer and `ModConfig.Load` turn an example into the real file only when it is absent. | — | Copying a package over an install can never replace the player's keybinds and flags. | Two installs in one night replaced Ron's keybinds and turned the dev menu off. | n/a |
| M01 after the prologue (live failure, September 10) | `M01.ExitPoint` in `locations.tsv` is the dockyard gate (1180, -2990) again; the row had the bible's address 838 m away, masked until the stale-template fix stopped honoring the shipped override. `ProloguePlacement.Prepare` no longer requires the exit's navmesh at the cold open: it is a drive-to marker in the last stage, not a spawn. | `M01.ExitPoint` estimate. | The prologue hands off into M01. | Log: "M01 could not find a loaded walkable surface for M01.ExitPoint" on every start attempt. | No. Retest the hand-off. |
| M19 site (live note, September 10) | The breach site is built from a hull: a frozen tug at the surface stands in for the Titan Star, the work point sits under its keel, the clamps under bow and stern, and the whole site moves out in 40 m steps when the seabed leaves no room under the keel. The hull is released on pass so chapter two hovers over it. | `M19.HullBreach` and the clamp rows are now the site's seed, not its markers. | Gohan has something to push against and can reach every mark. | Three markers in open water, sometimes under the seabed. | No. Retest the dive. |
| Port Heist as one operation | `MissionManager.Continuations`: M19 → M20 → M21 → M22 start as soon as the previous aftermath ends, no marker walk, no mission key. Each chapter still commits its own completion and rewards; a failure retries that chapter; a fresh session resumes at the next chapter's marker. M20 takes over the Kraken chapter one left on the surface instead of spawning a second. | — | The heist runs as one extended mission. | Four separately started jobs. | No. |
| Prologue car on the roof (live note, September 10) | `MissionSites.Ground` tries the authored height first and only asks the map for the ground when nothing walkable is near it. The September 10 ground snap probed from 150 m up and put the LSIA curb and the Primo on the terminal roof. | — | The car is on the street. | Ground snap from above. | No. Retest the arrival. |
| Crew van (Ron's direction, September 10) | `CrewVan`: the crew's own Granger, parked at `Stash.CrewVan` beside the Cypress base whenever the crew is deployed in free roam; anything done to it at a shop (mods, paint, livery, reinforced tires) is captured into the save when the player gets out or buys, and applied to every van the game creates. M02 and M06 spawn it instead of a stock Granger. The M11 fleet package applies on top through `FleetGarage`. | `Stash.CrewVan` estimate (874, -2116); `crewVan` in the save. | The Granger is the group's van: at the stash, in the missions, customizable, upgradeable. | Missions spawned fresh Grangers; nothing persisted. | No. |
| Operations declared | `MissionManager.Continuations` adds M44 → M48 and M63 → M70 (inert until scripted). `OPERATIONS.md` records every consecutive pair judged, the two staging links that wait on a gate warning, and the pairs left separate. | — | Back-to-back chapters on one clock run as one mission. | — | n/a |
| Story-to-play decisions (P1, September 10) | Recorded from Ron: Miller's fate is the player's (allowed either way, never required; neutral wording). Defaults taken as assumptions: keep the Primo and the current room (O02); keep the Granger with a stash beat (O03); the rail-junction ambush is a hired street crew (D-M03a); three dogs, no kill objective (D-M03b); M04 briefs at the base (D-M04a); an eight-second offered switch before it is forced (D-M04b); the prologue interior call after a live apartment pass (D-PRO). | — | One current plan per mission. | — | n/a |
| Scene spec, support cast, shots (P2a) | `SceneSpec` carries the reason, the support cast (speakers who are not brothers, played by the mission's own peds) and the blocking; `ShotStep` positions the scene camera itself (wide, over the shoulder, low, watching, with a push); `CarryPropStep`, `HandoverStep`, `InspectStep`, `TakeCoverStep`. Skip finishes them in order so a skipped handover still leaves the case in the right hand. `build_story.py` accepts `@phase` blocks with support speakers (MILLER, BUYER, MATEO). | `M04:transaction` (3 lines). | Scenes that show the action, not just the line. | Speaker framing only. | No. |
| Role tracks (P2a) | `RoleTrack`: Approaching, Observing, Working, Threatened, Covering, Extracting per brother while the player is someone else; combat, damage or a hostile within 35 m sends him to his cover point, six clear seconds resume the job; the active player's track is left alone; release hands the brothers back. | — | Inactive brothers have a job and react to danger. | Frozen poses under fire. | No. |
| Flow objectives (P2a) | `TravelObjective` with radio cues at fractions of the distance and stopped-in-zone arrival; `SwitchWindowObjective` offers the switch for a window while a shadow task keeps the job going, then requires it through the ordinary hand-off; `ReactionTrigger`; `WaitForRolesObjective`. `Objective.KeepsOwnerOpen` stops a stage forcing a switch the moment it begins. | — | Approach, offered switch, reactions as triggers. | Kill-count stage exits. | No. |
| Evidence, custody, endpoint, context card (P2a) | `CampaignState.Evidence` (Alleged / CopyHeld / Proven / Distributed; M01 ledgerLead, M02 dockRecording, M04 millerDrive); `Mission.Preserve` keeps a tracked entity through cleanup; `MissionEndpoint` on every composed mission, with `ClearHeatIfSafe` refusing a wanted reset except at a safehouse; `MissionContextCard` shows target, reason, roles and first destination for 7 s after a skipped briefing and a one-line recap on a retry; `Mission.Radio` for lines with no camera. | `evidence` in the save. | Custody survives a cut; no fake safety; a skipped briefing still informs. | — | No. |
| M04 reference (P2b) | Briefs at the base; the crew leaves in the crew van (Guess driving) on a travel objective with two radio calls; arrival drops Ice and Gohan onto role tracks while Guess holds the exit; the transaction scene shows Miller with the case, the buyer inspecting it, the guards, the van at the exit and Ice's angle; Gohan cuts the breaker and that is what makes Miller run (buyer flees, escort turns, Miller boards, flee mission); an eight-second offered switch to Guess while Ron's own AI chases; the pursuit; the reach-in pickup stores the drive and records the evidence; Miller alive runs, dead stays; the crew loses the police and regroups in the van with no wanted reset; the van and Miller's car remain. Start marker moved to the base. | `M04:transaction`; `M04_S2_03_GUESS` reworded; start key `Base.CypressFlats`. | The proposal's §4 sequence. | The old stage order. | No. The §4.3 comprehension test is the live gate. |
| M01 escape (live note, September 10) | Mateo is invincible for the mission; after recognition he holds near his spot instead of jogging the dock under fire; when the yard is clear enough the escape plays as a scene (`M01:escape`, Mateo speaks): he is already at the slipway, boards the launch, and it pulls away under a low tracking shot and a wide from the dock. Skip warps him aboard; the launch leaves on the next tick. The "route blocked" failure is gone. | `M01:escape` (2 lines). | Mateo reliably leaves; the escape is shown. | He ran the length of the dock, died or never arrived. | No. |
| Companion shield (Ron's direction, September 10) | `[Crew] CompanionsInvincible` (default on): the brothers the player is not controlling cannot be hurt; re-asserted every tick, taken off the moment one is switched into or the crew stands down. Their AI still takes cover and fights. | `CompanionsInvincible` in `Bloodlines.ini`. | Teammates do not die while the player is someone else. | Companions died out of view. | No. |
| M01 run and terminal (Ron's correction, September 10) | Recognition sends Mateo and his technician running for the launch on foot, in the open; Mateo cannot be hurt on the way, the technician can and it changes nothing. No clock: the yard has to be cleared. The launch waits with its engine off; the escape scene plays once the yard is clear and Mateo is at the boat (a Mateo the navmesh strands is brought to it after 45 s, logged). Once the launch has left, losing him at a distance is not his death. When Gohan finishes the terminal the player stays on Gohan; the objective line names the brother with a job left (the automatic switch back built here was removed the same day at Ron's request). The laptop rests on the table's top bound (`PropPlacement.OnTop`) in the cold open and the mission. The aftermath opens on a shot through Gohan's window (`Mission.OutroBlocking`). | — | Mateo's run is seen; the laptop is on the table. | The 90 s window and the cover hold. | No. |
| Prologue interior call (P2c, B9) | After the door, the home system streams the starter room with Ron held behind the fade; inside, he crosses the room, stops, the phone comes out and the three `M01:call` lines play (the last on his face). The door scene is now the door only. If the room never loads, the same call plays at the door and the player is told; skipping either way lands the job as read. The host leaves the room behind the fade before the cut to the dock. | `M01:call` (3 lines, Ron alone); `M01:arrival` is one line. | The job is read at home, not on a doorstep. | — | No; the first live apartment pass is still owed. |
| M02 stash beat, route, custody, endpoint (P2c) | Opens at the curb with the prototype (locked, left behind) and the crew on foot; the `M02:stash` scene boards them into the Granger (Guess, Ice, Gohan) and the upload clock starts only after it. The van runs a road mission far down its own road, re-aimed as it nears the target, with a stuck watchdog that re-issues once after five seconds stationary. Detection at 50% is the reaction that opens the windows. Gohan's work stalls the drivetrain; nothing rams the van. The drives leave the van in Ice's hand (`prop_ld_case_01`), `dockRecording` is recorded there, and they are stowed when he boards. The canal is an escape checkpoint: the police must be lost, nothing clears them. The Granger and the prototype remain. | `M02:stash` (3 lines). | The plan's M02 rows. | `CruiseWithVehicle`, the wanted reset at the marker. | No. |
| M03 as designed (P2d, repair plan Package D items 1–9) | From the base: the `M03:split` scene boards Ice and Gohan into the crew van and it pulls away; the cut lands them outside the depot on role tracks (Ice watching the gate, Gohan at the truck). Ron drives his Primo to the Davis junction on a travel objective with Ice's radio call; arrival is stopping in the zone, the work is a six-second hold on foot with no button, and rolling through does not eject him. Three mission-owned rottweilers are loose on the block before he arrives; five seconds into the work a hired street crew (Ballas models) comes out of the houses with a moment on the first of them. Ice's entry marker sits before the first guard; the guards patrol (neutral) until he reaches it; a `QuietRuleObjective` scoped to Ice within 140 m of the depot fails a shot before it with the reason, and Ron's junction fight is not in scope. Gohan opens the Benson (rear doors) and the `M03:loading` scene carries three real crates (`prop_box_wood02a`) from the pallet into the bed (`StowPropStep`); skipping leaves them in the bed; the doors close after. Ron takes the truck; the foundry is a `SafehouseArrival`: a `LoseWantedObjective` before delivery, no reset at the marker, `ClearHeatIfSafe` at the end; the delivered Benson locks (`CannotEnter`) and remains, as do the van and the Primo. The aftermath gets Ron out of the truck and frames it. The "No ability or button" caption is gone. | `M03:split` (2 lines), `M03:loading` (2 lines); `HoldZoneObjective(onFoot)`. | Package D items 1–9, B3/B4/B8. | The crane hold, the wanted reset at the base, the AI drive from the base to the depot. | No. Bed offsets and the block offsets are estimates. |
| Control after a ped change (Ron's September 10 log) | The prologue's cut to the dock and (until it was removed) Gohan's terminal hand-back both started a scene in the same tick as a player-ped change, when the engine reports control as off; the scene captured "off" and restored it, and the player could not move. Every `CHANGE_PLAYER_PED` site now asserts control (`GameUtils.AssertPlayerControl`: deployments, the story character's return, switches and rolled-back switches), the manager asserts it when gameplay begins and on the first mission tick after any scene, and the hand-off hands the briefing control that is on. Death recovery and the apartment fade keep their own hold. | — | Missions begin and resume with a player who can move. | — | No. |
| M01 after the terminal (Ron, September 10) | The automatic switch back after Gohan's copy is removed: the player stays on Gohan, sees "Ledger copied." and the standing objective line, and switches by hand to whichever brother still has a job. Ron reported the automatic switch misbehaving live; there is no ped change at the terminal now, so the recognition scene starts from a player who has been Gohan all along. | — | Nobody is moved off the brother they were playing. | `SwitchAfterTerminal` and the "Back to X" line. | No. |
| M01 HUD count (Ron, September 10) | The firefight line is "Hostiles: N" only; where Mateo is belongs to the objective line and his blip, not to a line that reads as the enemies announcing him. | — | — | "Mateo is running for the launch" on the count line. | No. |
| M05 as the plan (P3a) | The cove is seen before anyone moves (`M05:shore`: from the perch, from the dinghy, his boat under the lamps). Once Mateo is stopped on the water Ice comes down off the cliff to the shore on a role track, with a radio line. Gohan brings the dinghy alongside and takes him aboard; the questioning is the bible's own stage-two lines played as a scene (`CutsceneDirector.PlayStaged`) with Mateo aboard, over Gohan's shoulder, with the crew in one place; skipping leaves him in the boat. The mission records `mateoAllegation` as `Alleged`, not proof; the aftermath frames the dinghy with the four of them. The flare-from-boat adaptation stays; no glider, no sea cave. | `M05:shore` (2 lines). `PlayStaged`. | The proposal's M05 rows; M46 keeps the proof. | The 3 s "question from the dinghy" interaction and the unstaged stage-two dialogue. | No. |
| M06 as the plan (P3a) | The three positions are seen before anyone moves (`M06:positions`: Gohan under the feeder, Ice's angle on the Granger, the truck). The burn is Gohan's welding work at the racks; when it completes a real script fire burns where the work was (removed with the mission) and `vespucciBackup` is recorded as `Destroyed`. The first rotors over the roof turn Ron's waiting job into a pickup: a radio line, his own AI brings the Granger to the alley mouth if the player is elsewhere, then a stage for Guess to bring it there and a boarding stage where Ice and Gohan come to it on role tracks. The escape stays an escape checkpoint. The "No ability or button" caption is gone from the sally port. | `M06:positions` (2 lines); `EvidenceState.Destroyed`. | The proposal's M06 rows. | The extraction from the Granger's spawn spot. | No. The interior remains a candidate; the fire is at the exterior racks key. |
| A start from the mission menu (Ron, September 10) | The QA mission list starts the job at the job's own marker: the player is moved there under a short fade (`DevMenu.WarpToStart`, through `MissionMarkers.StartPoint` and `mission_starts.tsv`) before the briefing, so the crew's car pulls up to that street and the mission does not cut to a different place afterward. A marker that cannot stream leaves the player where they stand, logged. | — | The briefing at the job's place. | The briefing wherever the menu was opened, with the crew driving to the nearest street from there. | No. |
| The starter room loads (Ron's logs, September 10) | Every attempt on record timed out at "interior readiness" for interior 55042 with the player already inside it. Story Mode ships the Online apartments switched off: `ApartmentAccess` now reads `IS_INTERIOR_DISABLED` and `IS_INTERIOR_CAPPED`, logs both, enables and uncaps the room for the visit before pinning it, and puts both back on the way out. Entry also faces the player the way the entry's heading says. | — | Loading from inside the room. | — | No. The next log says "was disabled"/"was enabled" either way. |
| The room mapped out (Ron, September 10) | The starter room has named spots: `Apartment.Room.Door`, `Message`, `Wardrobe`, `Bed`, `Locker`, shipped as estimates and offered only once surveyed on foot (an estimate stays folded into the entry marker rather than sending anyone into a wall). Inside, each surveyed spot has its own marker and prompt: the wardrobe opens the wardrobe, the bed rests and saves, the locker restocks, the door leaves. The prologue's message is read at the surveyed message spot. With the dev menu on, inside the room: "Survey the room's spots" walks the six keys with the capture key, and "Map this room" writes `Bloodlines.Room.txt`, a half-meter floor map read from the room's own collision (`InteriorMapper`) with any movable props listed, for laying the spots out on geometry that exists. | — | The entry marker and its menu. | — | No. The spots are desk estimates until the room is surveyed; the map is the tool for that. |
| Helicopters stock (Ron, September 10) | The tuned rotorcraft was "just horrible", so helicopters are out of the travel system entirely: `WorldTuning` does not register them (no doubled ceiling, no cruise power, no forward push) and `TravelHandling` edits planes only. Planes, boats, bikes, submarines and trains keep the travel tuning; road cars keep theirs. | — | Stock helicopter flight. | The halved thrust falloff and longitudinal resistance, the 2x ceiling and the 3 m/s² push on rotorcraft. | No. A Buzzard should feel exactly as it does without the mod. |
| M07 as the plan (P3b) | The relay, the roof and Ron's sedan in the pickup lane are seen before the climb (`M07:approach`, 2 lines): the return path exists first. Ice clamps a real sniffer (a case prop) to the dish and it stays there; the dish is never destroyed. The manifests are read over the clamp as a scene from the bible's own stage lines (`M07:clamp`, `PlayStaged`): two turbine engines, Paleto, recorded as `aegisManifests` = `CopyHeld`, so the player knows how this job found M08's cargo. The helicopter is spawned far out and shown coming once (`PlayMoment` on its pilot) before the roof is Ice's problem; Ron's radio follows. The aftermath is a shot on the sedan. | `M07:approach` (2 lines). | The proposal's M07 rows; the accessible roof and the parachute exit stay. | The subtitle-only manifests; the helicopter that simply appeared. | No. The roof, the descent and the pickup lane are separate live tests, as the proposal says. |
| M08 as the plan (P3b) | The two crates (real props on their pads), the flatbed, the camera room and the sentries are seen first (`M08:approach`, 2 lines). Gohan's camera loop is a window: 240 s on the HUD, and it drops the job if the crates are still on the ground. Guess drives a real forklift to each crate; the lift, the drive to the bed and the set-down are staged and counted (`M08:loading`, one authored line per crate; `TransferPropStep`), so both crates are seen secured on the bed. The technical spawns up the street and is shown coming once (`PlayMoment` on its driver); Ice's fight and Ron's second crate share one stage in either order. Ron takes the flatbed, Ice boards its other seat, Gohan leaves his panel for the crew's Granger and follows. The police are lost before the stash. Delivery is a secured delivery, not a safehouse: nothing clears the wanted level. The flatbed and both crates are released to the world, locked, and the stash is recorded as save data (`CampaignState.Cargo`: `turbineEngines` at `M08.Connector`) for M09 to show and M10 to collect. | `M08:approach` (2 lines), `M08:loading` (2 lines); `CampaignState.Cargo`. | The proposal's M08 rows; M11 still owns installation. | The hold-zone "forklift", the technical that was simply present, the connector delivery that ended the engines' story. | No. Cab seats, cargo footprint and the loading route are estimates; the bed slot offsets are guesses. |
| M01 hostiles and escape (Ron's third round, September 10) | Every guard carries a red map mark that goes when he does. The escape scene plays the moment the yard is clear: a Mateo short of the launch is brought to it, and there is no stranded wait. | — | — | The 45 s wait for Mateo to reach the boat. | No. |
| M02 boarding (Ron, September 10) | Ice could not get back into the Granger with the drives in hand. At the escape the Granger is unlocked, the active brother's scripted state is released, and a press at its door (within 4.5 m) seats him in the first free seat. | — | — | — | No. |
| M03 junction, guards, Gohan (Ron, September 10) | The junction stage now also requires the street crew down (`ConditionObjective`), so Ron fights the ambush before anyone is asked to switch. No depot guard spawns within 7 m of the Benson; one that would is moved 9 m clear. At the foundry every brother is unseated and returned to his own AI before the truck locks, and the aftermath gets riders out as well as Ron. | — | — | The switch straight after the hold; the guard in the truck; Gohan stuck in the cab. | No. |
| M04 arrival (Ron, September 10) | Ron pulls up alone. Ice and Gohan are stationed at their positions from the start and their tracks are already Observing, so they are never seen arriving. The two radio calls say so. | — | The reference mission's other blocks. | Ice and Gohan riding in and walking to position in view of the meeting. | No. |
| M07 placement (Ron, September 10) | Ice's roof and the mast take the highest surface the world has at the estimate's X and Y (`RoofTop`, a ground probe from above), so he is on the building whatever the estimate's Z says; the sedan is placed on the nearest street to the lane estimate, not against a wall. | — | — | — | No. The XY estimates still want a survey. |
| M08 layout (Ron, September 10) | The crates are 14 m apart, the flatbed 26 m clear of them, and the forklift starts on the apron's street in the open rather than against the sheds. | — | — | The cramped layout. | No. Estimates. |
| Police sightings (Ron, September 10) | While the stars are grayed out, a cop with a clear line of sight inside 70 m reports the player and the search re-centers on them (`TacticalResponse.ReportSightings`, every 1.5 s). Break the line of sight and the search runs down as it always has: smarter, not unlosable. | — | The dispatch tuning. | — | No. |
| Progress meter (Ron, September 10) | "Stay in the marker: Ns" is gone. A hold or a piece of work shows its name and a very small meter above the subtitle line, drawn on the game's own timer-bar backing (`GameUtils.DrawProgressBar`; `MissionInteraction`, `HoldZoneObjective`). | — | — | The countdown text. | No. |
| Apartment tiers (Ron, September 10) | Ice and Guess had the same room. `ApartmentTiers` now gives each brother his own starter interior (Ice: the stock low-end room; Gohan: the stock mid room; Ron: the Forum Drive house, whose door becomes his home marker), keeps the three Eclipse penthouse floors as the luxury tier after M27, and adds a top tier after M47: the Diamond penthouse, two floors, shared, entered from the casino's front doors, furnished by interior entity sets that `ApartmentAccess` activates on entry and deactivates on leaving. Room spots are per room (`Apartment.Room.<Brother>.*`, 15 estimates); the room survey follows the active brother's residence; the prologue reads the message in Ron's own room. | — | The room system, the survey, the map. | The shared starter room; "Preview luxury apartment" (now "Preview the next residence"). | No. The Diamond's entity-set names and the house interior coordinates are unverified; a set the engine does not know is ignored. |
| M09 as the plan (P3c) | The convoy, the escort, Ice's rock and Ron's landing flat are seen before anyone moves (`M09:approach`, 2 lines). The escort must stop, not burn: a dead escort fails the job with the unit in it. When its driver drops, the colonel's vehicles are sent up the road and shown leaving once (`PlayMoment`), which is why nobody chases him. The unit comes out of the cab into Ice's hand as a scene from his own stage line (`M09:unit`, `PlayStaged`) and Gohan reads it remotely: one checkpoint, once, no invisibility. Ron lands on the flat past the culvert (`M09.Pickup`, a landed delivery), Ice boards, and the unit is recorded at the drop (`Cargo`: `iffTransponder`). A secured delivery: nothing clears the wanted level. | `M09:approach` (2 lines). | The proposal's M09 rows; "the colonel isn't family" stays in the briefing. | The skids on the cab roof; the subtitle-only code. | No. |
| M10 as the plan (P3c) | M10 starts at the connector stash (`mission_starts`: `M08.Connector`) with the same two crates on the flatbed where M08 left them, checked on camera (`M10:inspect`, 2 lines) with Ice's seat and Gohan's role said in the yard. Ice rides the passenger seat. The Buzzard spawns behind and high and is shown coming once; the tunnel mouth is named as the firing window, the truck goes there (Ron's own AI holds it) and Ice steps out with the launcher: the truck is never frozen in traffic. The police are lost and the engines delivered to the Burro Heights shop, recorded there (`Cargo`: `turbineEngines` at `M11.ChopShop`), the truck shut down and released with its crates; the aftermath is Ron out of the cab over the load. | `M10:inspect` (2 lines). | The speed floor, the bikes, the RPG. | The Del Perro start with no crates; the frozen truck; the tunnel delivery. | No. |
| M11 as the plan (P3c) | No briefing on arrival: the shop mid-work is the opening scene (`M11:shop`, 2 lines): Ron welding at the bay, Ice with the clipboard, Gohan walking up; the flatbed at the shop with the second crate still on it, the first "in the bay". The calibration is kept and shows the meter. When the engine is done, Gohan walks to the Granger's window and Berth 44 is the bible's stage-four lines staged in the room (`M11:berth`, `PlayStaged`); three billion is said to be a manifest figure, not cash. The aftermath gets Ice out and shows the truck. The second engine is recorded kept at the shop. | `M11:shop` (2 lines). | The dyno, the profile reward. | The stage-four lines as a radio debrief. | No. |
| SM01 as the plan (P3d) | Sergei, the two crates (real props) and Ice's own car are seen before the side door (`SM01:approach`, 2 lines; Ron on the radio). The broker must stay alive for the codes: a dead Sergei fails the job; once the codes are given he runs, invincible, out of the story. Both crates are carried into Ice's car on camera and counted (`SM01:crates`, staged from his own stage line). Ice drives them to his own door, a safehouse arrival; the crates are recorded there (`Cargo`: `sergeiCrates`). The reward is said as it works: the Pump Shotgun Mk II in his locker and double rifle ammunition on restock, which the locker already did. | `SM01:approach` (2 lines). | Ice's restraint; mission independence; no crew on the floor. | The invisible cargo; the reward line that claimed an armor-piercing damage mechanic. | No. |
| SM02 as the plan (P3d) | The roof approach, the server bay and a real terminal (a table and laptop at the key) are seen first, with the twenty-minute check-in said and Ice answering that they listen, not come (`SM02:approach`, 2 lines). The guards must survive to the end, as before. The tap is a scene at the terminal (`SM02:terminal`, staged from the stage line) and records `cameraArchive` = `CopyHeld`, said to be municipal camera access and not the dock recording or a witness. IT's trace is a 75 s clock on the fire escape. Gohan radios that he is returning; the reward is said as the Marksman Rifle. An escape checkpoint: nothing clears the wanted level. | `SM02:approach` (2 lines). | The stun rule; Thermal Pulse. | — | No. The office and fire escape remain candidates. |
| SM03 as the plan (P3d) | KJ walks the prize (a crate on the coupe's tail) and the coupe, and looks at the rivals' Elegys, with the warning said before the grid (`SM03:approach`, 2 lines, KJ speaking). The race is unchanged and nothing auto-wins a lap. After the result the rivals' guns are shown once (`PlayMoment` on the first shooter) and Ron's line follows; the either/or stands. A new final stage drives the coupe and the prize to the Burro Heights chop bay, a safehouse arrival, where the prize is recorded (`Cargo`: `racePrize`) and the reward said as it works: $25,000 and the race transmission the fleet garage fits. KJ's check-in is the aftermath over the phone; he never enters the combat path. | `SM03:approach` (2 lines). | The circuit, the either/or ambush, KJ as a contact. | The reward claimed at the finish line. | No. |
| M12 as the plan (P4a) | The hold number, the sub on the jetty, the freighter over the water and Ice on the pier are seen before the dive (`M12:approach`, 2 lines): Gohan is the one going under. The scan is a scene at the hull from his own stage line (`M12:survey`). The two launches, full-tanked, are shown once as the next problem (`PlayMoment`). The survey is recorded (`Evidence`: `hullSurvey` = `CopyHeld`): reachable hull, not ready heist. A secured delivery. | `M12:approach` (2 lines). | The stealth rules; the sub as the ROV. | — | No. |
| M13 as the plan (P4a) | The three barges, the slipway men named as dock workers (their own group, not the cartel's) and Ron's dark Granger (the crew's own) are seen first (`M13:approach`, 2 lines). The plants use the meter. When the third charge is set a launch turns into the basin and is shown once: the reason to leave. The charges stay dark until Ice is aboard; the detonation is followed by a result view from the slipway (`M13:result`, from Ice's line). `harborPatrolsReduced` is recorded for M21 to consume; nothing claims an empty harbor. An escape checkpoint. | `M13:approach` (2 lines). | Three sites, the player's order, the detonation as the reward. | — | No. M21 consuming the flag is P4c. |
| M14 as the plan (P4a) | The Besra with the pod, the apron guards, Ron below the hangar and Ice on the ridge are seen first (`M14:approach`, 2 lines), with McKenzie named as the destination. Once Ron is airborne, Ice and Gohan leave the ridge for the crew's Granger on role tracks and say so; nobody shares a seat the plane lacks. A radio cue at the midpoint of the low route. Landed and stopped, the pod is shown under the wing (`M14:pod`, from Ron's line), the plane released, and `radarPod` recorded at `M14.McKenzieHangar` for M18 and M20. One radar window, not a blind Aegis. A secured delivery. | `M14:approach` (2 lines). | The ceiling; the stolen-aircraft route. | — | No. M18/M20 consuming the pod is P4b/P4c. |
| M15 as the plan (P4a) | The service access, a watchman's round, a real cable point (a case at the splice key) and Ron's Granger at the exit are seen first (`M15:approach`, 2 lines); Ron is stationed in it. The splice is a scene at the cable point (`M15:splice`, from Gohan's line). One gate answering is the acknowledgment, over the radio: not a harbor gone dark. `harborGateAccess` is recorded for M21, said to be three copies. An escape checkpoint; the watchmen survive to the end. | `M15:approach` (2 lines). | The stun rule; Thermal Pulse. | — | No. M21 consuming the access is P4c. |
| M16 as the plan (P4b) | The IFF unit (a real prop on the crew's Granger at the fence), the Cargobob on the pad, Ice's cover and the canyon exit are seen first (`M16:approach`, 2 lines). The loss of the clearance is a challenge on the net (radio) as the pad is crossed, then the base's heat; the transponder is spent (`Cargo` cleared). Ron in the lift: Ice boards it (a bounded wait, then seated, logged) and Gohan takes the Granger out by road; nobody shares a seat the aircraft lacks. The pursuit is lost before Terminal (`LoseWantedObjective`), not cleared at the marker. The lift landed is a scene (`M16:lift`, 2 lines) and recorded (`Cargo`: `cargobob` at `M16.TerminalDrop`). A secured delivery. | `M16:approach`, `M16:lift` (2 lines each). | The ceiling; the canyon. | The wanted reset at Terminal. | No. The Lazer bridge for M26 is P5a. |
| M17 as the plan (P4b) | The sub from the survey in the slip, Ron looking it over, Ice at the pickup gear (a crate), seen first (`M17:approach`, 2 lines). Each weld point leaves a real part on the hull (`MultiHoldObjective.SiteDone`): three sites, three parts. Ron asks where the release is (radio); Gohan moves it to the outside as a handle prop, on camera (`M17:release`, 2 lines), where either brother can reach it. The sub is recorded ready (`Cargo`: `kraken` at `M17.DrySlip`). A secured delivery. | `M17:approach`, `M17:release` (2 lines each). | The quiet mission. | — | No. |
| M18 as the plan (P4b) | The plan on the hood is seen first (`M18:approach`, 2 lines): the sub's route, the lift point, the water route, the inland stash, one operator per vehicle. Open solo jobs are called out at the start. Each delivery is recorded (`kraken`, `cargobob`, `hauler`); the pod from McKenzie is fitted to the Cargobob as a real part and recorded on it, so M20 has a thing to switch on. The roll call is the three stage lines staged with each man in his own seat (`M18:rollcall`, `PlayStaged`); one clock is recorded (`heistClock`). The endpoint is continuous: M19 follows under the same clock. | `M18:approach` (2 lines). | The three deliveries by three brothers. | The roll call as bare cues. | No. |
| M19 as the plan (P4c) | A brief cut into the operation as staged (`M19:approach`, 2 lines): the Kraken taken over at its recorded location (`Cargo kraken`: M18's channel, M17's slip, or the dive mark), Ice on the pier, Ron sitting in the lift at the salt hangar with the engine off. The whole crew deploys; nobody is dropped on open water. Each clamp leaves a real float at its site (`MultiHoldObjective.SiteDone`); the last one brings the container up as a staged insert (`M19:float`, Ice's stage line) beside the surfacing mark, floats on it; skipping places it the same. The surfaced sub, the container and the lift are recorded (`Cargo kraken`, `bullion`; handoff notes) and released for M20. Endpoint continuous. | `M19:approach` (2 lines); `M19:float` (1 stage line). | The breach, two clamps, the surfacing. | A solo deploy onto water; three markers and a subtitle. | No. |
| M20 as the plan (P4c) | The live handoff seen first (`M20:approach`, 2 lines): the container on its floats where M19 left it (the same prop when it is there), the five quay gunners where they stand, Ice on the pier, the lift on the apron. The lift, the sub and the container are taken over from M19, not duplicated. The pod gets its one job: lifting off, Gohan's radio switches it on for the window (or says there is none), from `Cargo radarPod`. The hook is a staged insert (`M20:hook`, Ron's cable line) that attaches the container and makes the aircraft fly heavy. After the climb-out, Ice and Gohan board the escort launch on camera with actual seats (`M20:transfer`, Ron's stage line plus two radio lines) and the Kraken's storage is said once and recorded at the pier. Shore cover stays the plan. Endpoint continuous. | `M20:approach` (2 lines); `M20:hook`, `M20:transfer` (staged). | Ice clears the quay; Ron holds the hover and flies the climb-out. | A second Cargobob spawned beside M18's; the transfer unshown. | No. |
| M21 as the plan (P4c) | Gohan at the helm, Ice in the launch's other seat and Ron's loaded lift held over the basin, seen first (`M21:approach`, 2 lines); the launch, the lift and the container carry on from M20. The harbor as the preparation left it: `harborPatrolsReduced` (M13) leaves two Aegis launches instead of three; `harborGateAccess` (M15) holds them beyond the breakwater gate instead of at it; both said once on the radio and logged. At the breakwater the split is announced: the lift is tasked north over the ridge, the launch turns for a shore landing (`M21.ShoreLanding`). The road transfer is a scene (`M21:transfer`, two radio lines): out of the launch, into the crew's Granger at `M21.RoadPickup`, Gohan driving, Ice beside him, pulling away. Recorded for M22. Endpoint continuous. | `M21:approach` (2 lines); `M21:transfer` (staged). | The escort, the boats, the breakwater, the shore landing. | Three boats regardless; the crew vanishing after the breakwater. | No. |
| M22 as the plan (P4c) | The arrivals, not a plan (`M22:approach`, 2 lines): the shallow drop point, the lift held over the water with the container, the Granger from M21 driven down the road with Gohan at the wheel and Ice beside him, both out on the beach. The drop is a staged insert (`M22:drop`, Ron's line); the hidden cargo is recorded once (`Cargo bullion` at `M22.AlamoDrop`, never on a replay). After landing and shutdown, a real regroup on foot. The strike is learned before it is explained (`M22:strike`): the alarm line on Ron's phone, a cut to the detonation at the foundry's real coordinates, the column from the beach, Gohan at the laptop on the hood, then Ron with the three keys from M03 in his hand (one reaction, one next step) and the keys stowed in the Granger. The foundry's loss commits once in `MarkComplete`; a replay never resets the salvage ledger. One strike source: Gohan's contract-airframe line. | `M22:approach` (2 lines); `M22:drop`, `M22:strike` (staged from the stage lines). | Hover and release, land, walk to the others. | Five speeches in a lineup; the strike as a subtitle after a fade. | No. |
| M03 keys (P4c) | The aftermath now has Ron put three keys down on the locked Benson (`p_car_keys_01`, carried then stowed on the truck) after he gets out and before the lines: the object M22 hands back to him. | M03 outro blocking. | — | No keys anywhere. | No. |
| Skipped shots (P4c) | A `ShotStep` that carries an action runs it when the scene is skipped before the shot started, so the float, the detonation and any later insert leave the same state watched or skipped. | — | — | Skipping a shot dropped its action. | No. |
| M03 depot ambush (Ron, September 11) | The Benson spawns on the nearest road node to `M03.HaulerSpawn` (within 40 m) and is placed on the ground, so it cannot stand in a wall; the depot team's marks derive from where it actually is. The junction block has four dogs, close in, with combat attributes set, no permanent-event block, and a fight order re-issued every few seconds when a dog is not fighting; the street crew is six; the depot guards are ten. While the block is Ron's fight, the street crew and the dogs are marked red on the map. The loading ends with Gohan in the cab; the depot answers: seven gunmen and three dogs come through the gate onto Ice and Gohan (Gohan out of the cab, both in cover), Ice's radio calls Ron back, and the road back is Ron's alone (`PlayedBy`). With Ron within 45 m the fight is anyone's (`AnyOf`, red markers). Then Ron drives with Ice riding (a bounded wait, then seated); Gohan takes the van behind on his own AI. The aftermath gets Ron and Ice out of the truck before the keys go down. | — | The return, the joint fight, Ice aboard. | Truck in the wall; dogs that wandered; no map markers on the block's crew; nobody came back for the depot. | No. |
| M23 as the plan (P5a) | The crew arrives in its own Granger with what it has left; the exterior survey is seen first (`M23:approach`, 2 lines): Ice reading the occupied approach, Ron walking the fence for a second exit (`M23.SecondExit`), Gohan looking at the generator house. During the clearing the other two take cover on role tracks; nobody teleports into the yard. Each bay is looked into (`MultiHoldObjective.SiteDone`): what is there and what is missing, said once each. The generator is started by hand as a staged scene (`M23:power`, Gohan's line); the four limits (power on a tank, no fuel reserve, no tools, no cash) are named as the reasons for M24 and M29 (`bunkerGenerator` flag). A quiet walk to the door ends it; the outro lines follow. Endpoint: safehouse arrival. | `M23:approach` (2 lines); `M23:power` (staged). | The dome, the clearing, three bays, the generator, the walk. | Three brothers dropped at the fence; the base as a subtitle. | No. |
| M24 as the plan (P5a) | The truck looked over by Ron, the container still in the Alamo water (`prop_container_01a` at `M22.AlamoDrop`), Ice on the ridge, Gohan in the shallows at the container with the truck as his way out, seen first (`M24:approach`, 2 lines). The recovery is a staged lift: two crates out of the container onto the bed (`M24:hoist`, Ron's line). The deputies come by the ridge road in cruisers (`M24.RidgeRoad`), one then two, after Ice's warning line, and get out to fight when they reach the shore; they are bought men, blipped as such, not a police force. Gohan boards the truck on camera before it rolls (a bounded wait, then seated). At the bunker the crates go into bay one as a scene (`M24:unload`, Gohan's line), recorded (`recoveredGold` at `M23.BayOne`) with what is left in the Alamo said once; the gold and cash still commit only in `MarkComplete`. Endpoint: safehouse arrival. | `M24:approach` (2 lines); `M24:hoist`, `M24:unload` (staged). | Park, work the cable, hold the ridge, drive home. | Deputies spawned standing on the ridge; the load invisible; Gohan in two places. | No. |
| M25 as the plan (P5a) | The whole crew deploys: Ice at the north end of the deck (`M25.DeckApproach`) walking it, Gohan on the rim with the route (`M25.RimPost`), Ron in the boat under the span with the engine running, seen first (`M25:approach`, 2 lines): the pickup exists before it is the only answer. After the waves, the encirclement and the way down to the boat are shown as a scene (`M25:escape`, Ron's line) before the jump is asked for; the jump is the player's, with one radio line talking him down. Ice boards; the outro connects the spotters to the water. Endpoint: escape checkpoint. | `M25:approach` (2 lines); `M25:escape` (staged). | The deck, the tanker, three waves, the jump, the boat. | A solo deploy; the boat a marker; the jump demanded cold. | No. |
| M26 as the plan (P5a) | The spotter pattern over the water, the Lazer parked on the McKenzie apron with its history (the intro line: towed out of Zancudo under the M16 clearance), Gohan at the laptop on the Granger's hood, Ice at the hangar door, seen first (`M26:approach`, 2 lines). After the first spotter, Gohan asks for the second held while he pulls the charter's call sign from its traffic (a 12-second listen; splashing it early fails the job with the reason); the call sign is recorded as evidence held (`charterCallSign`). The Lazer is parked and shut down as a scene (`M26:park`, Ron's line) beside the Duster, the two-seat aircraft the next job flies, so the change of aircraft is a thing on the apron; recorded (`lazer` at `M26.DusterPad`). Endpoint: secured delivery. | `M26:approach` (2 lines); `M26:park` (staged). | Scramble, two kills, the listen, the landing. | The Lazer appearing because the mission needed it; the lead learned from wreckage. | No. |
| M27 as the plan (P5a) | The approach aircraft is the Duster (two seats, radial engine); the Mallard has one seat and could not carry Ice. At the apron the Lazer is parked from M26 beside it; Ice checks his parachute and boards the second seat on camera; the Shamal's track and Gohan already at sea in the boat are shown (`M27:approach`, 2 lines). The transfer is a staged cut (`M27:transfer`, Ice's line): the two aircraft in formation, a fade, Ice in a real Shamal seat as the player; skipping leaves the same state. The ledger is a case in Ice's hand and evidence held (`flightLedger`); Ron's Duster is tasked home to McKenzie on its own route with a radio line; the bail-out is the player's; in the boat the case is stowed where Gohan can see it. Ron's spoken line still calls the Duster "the stunt plane"; that is his word for it. Endpoint: escape checkpoint. | `M27:approach` (2 lines); `M27:transfer` (staged). | Take off, hold station, the locker, the bail-out, the boat. | A one-seat plane with an unshown passenger; a `Script.Wait` in the transfer. | No. |
| Multi-site hold meter (P4a) | `MultiHoldObjective` shows the meter and "N left", not a countdown. | — | — | The countdown text. | No. |
| Apartment fade (Ron, September 10: "under the map") | A scene ending in the same breath as the room entry could fade the screen back in over a room still streaming, so the player watched the void from inside the interior. `ApartmentAccess` now holds the fade itself until the room is ready, logging once if it had to. | — | — | — | No. |
| Aircraft (Package B, note 19) | `TravelHandling.Report` says per model which flight properties were found and applied or "unsupported on this runtime"; `WorldTuning` logs it. Helicopters were given a bounded forward force here; that and every other rotorcraft edit were removed on September 10 (see the helicopters row). | — | "No faster" becomes a number in the log, and a Buzzard cruise gain exists on this SDK. | The flight adapter silently did nothing on 3.6. | No. Paired Buzzard runs pending. |

## Economy ledger (audit §13)

One vocabulary for every form of wealth, so no mission can call the same score
three names:

| Asset | Save field | Set by | Consumed by |
| --- | --- | --- | --- |
| Physical bullion taken | narrative: 30 t | M20 lift | — |
| Physical bullion hidden | 30 t minus `alamoGoldDredgedTons` | M22 drop (`AlamoGoldDredgedTons = 0`) | M24 (+5 t) |
| Physical bullion recovered | `alamoGoldDredgedTons` | M24 | future Act II/III conversions |
| Liquid operating cash | `cashOnHand` | first completions (M05 … SM06), shops | shops, customization |
| Bearer bonds / rig loot | not yet a field | M46 (unscripted) | — |
| Electronic / offshore | `offshoreEscrowBalance` | none yet (zero in playable chapters) | — |

The "$3 billion" in M11 is a manifest claim, not spendable money, and stays that way.

## Antagonist and support presence map (audit §16)

| Character | First mention | First appearance | First action that hits the trio | Mid-campaign reminder | Final payoff |
| --- | --- | --- | --- | --- | --- |
| Mateo | M01 intro | M01 (clipboard, dock) | Runs at recognition; his ledger is the lead | M05 capture and allegation | Names the contracts; his word starts the hunt, M46 finishes it |
| Miller | M03 outro | M04 | Sells the dock forensics | — | Drive points to Mateo's cave |
| Vance | M08 outro (shared surname) | M09 convoy (unseen) | Convoy IFF is the crew's way in | M16 burns the clearance; M26 charters | M65 confrontation (unscripted): must attack something specific about the trio |
| Ramos | M32 (unscripted) | M32 | Rescued as a person first | M33 stabilization before codes; M35/M43 cited | Codes open the rig; stays a person |
| Harrison / Bradley | M44–M46 (unscripted) | — | — | — | Bradley's card opens the helipad route (M45) |
| Sterling | SM07 (unscripted) | — | Personal to Ice | — | SM07: "Sterling's dead." / "You good?" / "Nah." |
| KJ | SM03 intro | SM03 | Watches the rivals' line | — | SM09 (unscripted): warns Ron to keep a backup exit |

## Continuity flags carried forward

- M39 rig communications: choose cut-to-backup (slower, noisier) over flatlined; enemy behavior must follow. Not yet scripted.
- M51 blackout timing: choose one of blackout now / test outage / charges staged; the tower sequence must respect it. Not yet scripted.
- M60 civilians: any large explosive event needs evacuation or placement justification. Not yet scripted.
- Evidence chain: M05 allegation → verification → M46 proof → M59 publication → later records deny containment. M05 now conforms.


## Continuous Port Heist (adopted September 11)

Built on `codex/continuous-port-heist` from `bb073aa` and merged onto main after the desert slice (P5a). Ron's decision: one continuous mission rather than four chapters with checkpoints between them. The chapter scripts and IDs stay for QA starts and old saves.

- M19-M22 now run under `PortHeistOperation` in normal play. Their IDs/classes remain available for QA and old saves.
- One live world, one initial briefing/loan session, one final mission result. Phase boundaries do not invoke the ordinary mission Start/Finish path.
- Exact aircraft/cargo/crew references survive live transitions; a missing essential asset fails instead of being replaced by a nearby copy. M21-to-M22 includes real inland flight and the road team's travel.
- Required scene results, seat claims and prop attachments are verified; transfer/arrival flags no longer certify a started scene as completed.
- A separate phase-start bookmark supports cold retry/resume without committing intermediate mission rewards. This is NOT arbitrary mid-stage checkpoint restoration; that remains disabled.
- Legacy completion/reward records are retained. Full operation success commits remaining phase IDs and final cargo once. Replays cannot reset later recovery or duplicate payment.
- See `CONTINUOUS-PORT-HEIST.md` for limitations and the live comparison route. Source results are in `CONTINUOUS-PORT-HEIST-VERIFICATION.md`; no live GTA verification is claimed.
